# AI Chat History: Backend Testing & SQLite Shim Implementation

**Date:** 2026-03-11  
**Time:** 3 PM - 4 PM  
**Conversation ID:** 9b434566-a450-43fa-9efd-3265577ff8e9  
**Goal:** 实现 >80% 后端测试覆盖率，处理 PostgreSQL ARRAY 的 SQLite 兼容性，并建立 CI 工作流。

---

## 📋 总结 (Summary)

本次对话完成了 SkillGap 项目后端的全套单元与集成测试。核心挑战在于生产环境使用 PostgreSQL 的 `ARRAY` 类型，而为了保证 CI 的轻量和测试的隔离性，我们选择了 **SQLite In-Memory** 数据库进行测试。通过编写一个高度定制化的 `ARRAY` Shim (Monkey-patch)，成功解决了方言差异，使得 103 个测试全部绿灯通过，覆盖率达到 **96.96%**。

---

## 🛠️ 测试进展与状态追踪 (Testing Progress & Status)

### 1. 基础设施搭建
- **Action**: 创建了 `.github/workflows/ci.yml`，设置了 Lint -> Test -> Build 的多阶段流水线。
- **Action**: 创建了 `server/pyproject.toml`，统一了 `ruff`, `black`, `pytest`, `coverage` 的配置。
- **Action**: 创建了 `server/tests/conftest.py`，配置了 SQLite 内存数据库、事务回滚机制和 FastAPI `TestClient`。

### 2. 测试用例开发 (TDD 流程)
- **New Tests**: `test_auth.py` (20 cases) - 登录、注册、Token、/me。
- **New Tests**: `test_extraction.py` (35 cases) - 技能提取算法、匹配比分计算、API 端点。
- **New Tests**: `test_profile.py` (19 cases) - 个人简历提取、技能增删查。
- **New Tests**: `test_history.py` (22 cases) - 历史记录 CRUD、权限校验、分数校验。
- **New Tests**: `test_claude_integration.py` (7 cases) - Roadmap 结构验证，并标记了 `@integration` 以在 CI 中跳过真实 API 调用。

### 3. 测试演进与调试日志 (Testing Evolution & Debugging Log)

本次任务严格遵循 TDD 流程，经历了从“全面报错”到“逻辑失败”再到“全量通过”的演进过程：

| 运行阶段 | 测试总数 | 状态 (Status) | 关键错误/现象 | 解决动作 (Action) |
| :--- | :--- | :--- | :--- | :--- |
| **第一轮** | 103 | **103 ERROR** | `TypeError: %d format: a real number is required...` | 修复 `pg_dialect.ARRAY` 补丁，从类替换改为工厂函数。 |
| **第二轮** | 103 | **7 FAIL / 33 ERROR** | `ResponseValidationError`: `skills` 字段无法解析。 | 优化 `JsonEncodedList` 兼容 PostgreSQL 的 `'{}'` 语法。 |
| **最终轮** | 103 | **103 PASSED** | ✅ 所有基础/逻辑/边界/端点测试全部绿灯通过。 | 最终覆盖率验证达到 **96.96%**。 |

#### 典型 Bug 修复详情 (Debug Details):
1. **ARRAY 兼容性 (TypeError)**: SQLAlchemy 在构建 SQLite 表时，`ARRAY(String)` 模式下会把 `String` 类作为参数传给 `TypeDecorator`。改用 `_array_factory` 后，成功拦截并返回了正确的实例。
2. **空数组解析 (Validation)**: PostgreSQL 的 `server_default='{}'` 在 SQLite 中被当成字符串存储，JSON 解析后变成 `{}` (dict)。增加判断逻辑，将 `{}` 解析结果强制转换回 `[]` (list) 以匹配 Pydantic schema。


---

## 📄 核心代码记录 (Key Code Snippets)

### 1. The ARRAY Shim (conftest.py)
```python
def _array_factory(*args: object, **kwargs: object) -> JsonEncodedList:
    return JsonEncodedList()

# Monkey-patch to allow SQLite to handle Postgres-specific ARRAY columns
pg_dialect.ARRAY = _array_factory
```

### 2. Final Coverage Report
```text
Name                                      Stmts   Miss  Cover
-------------------------------------------------------------
server/auth/router.py                        30      0   100%
server/extraction/engine.py                  35      0   100%
server/history/router.py                     35      0   100%
server/profile/router.py                     52      0   100%
...
TOTAL                                       986     30    97%
```

---

## 🗣️ 对话关键节点 (Conversation Log)

- **USER**: 我们需要使用 pytest 实现 80% 以上的后端测试覆盖率。请基于当前的 FastAPI 路由 和 数据库模型，创建带有数据库 fixtures 的 conftest.py。我们正在使用 TDD。
- **AI**: 好的，我会先建立 `conftest.py` 并处理 PostgreSQL `ARRAY` 类型与 SQLite 的冲突。
- **AI**: (遇到错误) 发现 `ARRAY(String)` 在 SQLite 下会报错。这是因为 SQLAlchemy 的 `TypeDecorator` 接收了类对象而不是实例。
- **AI**: (修复) 我改用了工厂模式。现在所有 Auth 和 Extraction 测试都过了。
- **USER**: 好的，请继续完成 Profile 和 History 的测试。
- **AI**: 已完成 `test_profile.py` 和 `test_history.py`。
- **AI**: (最终运行) 全套 103 个测试已通过。覆盖率 96.96%。
- **USER**: 请问你现在是在用SQLite做测试吗?但我真实的数据库用的是Render的PostgreSQL...
- **AI**: 详细解释了 SQLite (隔离/快/免配额) vs Render Postgres (真实/但有配额风险) vs Docker Postgres。
- **USER**: 谢谢,那我选择Option A：继续 SQLite。

---
*Generated by Antigravity IDE AI - 2026-03-11*
